name: CI Deploy

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Xmake
        uses: xmake-io/github-action-setup-xmake@v1
      - name: Build Linux
        run: make package
